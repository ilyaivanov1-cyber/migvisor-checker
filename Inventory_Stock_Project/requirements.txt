# =============================================================================
# Project  : Inventory_Stock_Project
# Product  : Purchase
# Purpose  : Python runtime and test dependencies for the Purchase ETL product.
#            Pin minor versions to match Databricks Runtime 13.3 LTS.
# =============================================================================

# ---------------------------------------------------------------------------
# Runtime — required on the Databricks cluster (DBR 13.3 LTS pre-installs
# pyspark and delta-spark; list here for local dev and CI reproducibility)
# ---------------------------------------------------------------------------
pyspark==3.4.1
delta-spark==2.4.0
pyyaml>=6.0,<7.0

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0

# ---------------------------------------------------------------------------
# Code quality (optional — used in CI lint stage)
# ---------------------------------------------------------------------------
flake8>=6.0,<8.0
sqlfluff>=2.3,<4.0
