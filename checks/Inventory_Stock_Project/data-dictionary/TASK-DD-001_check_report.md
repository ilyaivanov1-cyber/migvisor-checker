# Check Report: data-dictionary — Inventory_Stock_Project / Purchase

**Skill:** migvisor-task-checker-data-dictionary (#11 of 21)
**Trainee:** Inventory_Stock_Project
**Product:** Purchase
**Checked:** 2026-09-25
**Score:** 85 / 100
**Grade:** Good

---

## Cross-File Reads

| File | Status | Notes |
|---|---|---|
| `codebase/docs/design.md` | Read | DDL for bronze.lineage_run, bronze.purchase_staging, silver_fact.fact_purchase verified; dq_rejections column names from to-be.md ER diagram used for reference |
| `specifications/to-be.md` | Read | ER diagram confirms: dq_rejections uses rule_id, violation_column, violation_value (trainee-aligned); silver_dim.date is "externally owned" — not a Purchase product table |

---

## Rubric Sections

### Section 1 — Table Coverage

**Score: 28 / 35**

The data-dictionary covers 7 tables:
1. `silver_fact.fact_purchase` (11 columns) ✓
2. `bronze.purchase_staging` (15 columns) ✓
3. `bronze.lineage_run` (9 columns) ✓
4. `bronze.etl_cutoff` (3 columns) ✓
5. `bronze.dq_rejections` (10 columns) ✓
6. `silver_dim.supplier` (18 columns) ✓
7. `silver_dim.stock_item` (20 columns) ✓

Cross-file verification: All 7 tables are present in design.md DDL and to-be.md ER diagram. Column counts match DDL definitions.

Deduction (-7): `silver_dim.date` is not documented. The to-be.md classifies silver_dim.date as "externally owned — not owned by the Purchase workflow," which partially justifies its exclusion. However, the Purchase product has a FK dependency on silver_dim.date (fact_purchase.date_key → silver_dim.date) and the table is a required input for SK resolution and FK validation. The reference data_dictionary.md documents silver_dim.date (14 columns) as a dependency table. Documenting externally owned input tables in the data dictionary is best practice for data product consumers and is expected by the rubric's table-coverage criterion.

### Section 2 — Column-Level Documentation

**Score: 32 / 35**

The trainee uses a 6-column format: Column Name, Data Type, Nullable, Description, Business Meaning, Derivation/Source. This is richer than the reference's 4-column format (Column, Type, Nullable, Description). The extra columns Business Meaning and Derivation/Source add significant documentation value.

All documented columns have non-empty values in all 6 fields. Descriptions are accurate and align with design.md DDL COMMENTs. Business Meaning explanations are substantive (e.g., for lineage_key: "Links every row in this table to the specific ETL run that produced it").

Deduction (-3): Three tables (bronze.etl_cutoff, bronze.lineage_run) have slightly thin Derivation/Source entries that say "Generated by system" without specifying the generating notebook or helper function. The design.md and to-be.md provide specific detail (open_lineage_record(), set_etl_cutoff()) that should appear in the dictionary.

### Section 3 — SCD-2 Tracking Columns

**Score: 15 / 15** (BONUS/REQUIRED)

All 5 SCD-2 tracking columns are present and fully documented in both silver_dim.supplier and silver_dim.stock_item:
- `valid_from` DATE — ✓
- `valid_to` DATE — ✓
- `row_effective_date` DATE — ✓
- `row_expiry_date` DATE — ✓
- `is_current_row` BOOLEAN — ✓

Types are DATE (not TIMESTAMP_NTZ), which is correct per TY-P001 override rule documented in to-be.md. This matches design.md DDL exactly.

### Section 4 — FK and Key Documentation

**Score: 10 / 10**

All foreign key relationships are documented in the specific format `table.column`:
- `bronze.dq_rejections.lineage_key` → "FK to bronze.lineage_run.lineage_key" ✓
- `silver_dim.supplier.lineage_key` → "FK to bronze.lineage_run.lineage_key" ✓
- `silver_fact.fact_purchase.supplier_key` → "FK to silver_dim.supplier.supplier_key" ✓
- `silver_fact.fact_purchase.stock_item_key` → "FK to silver_dim.stock_item.stock_item_key" ✓
- `silver_fact.fact_purchase.date_key` → "FK to silver_dim.date" ✓
- `silver_fact.fact_purchase.lineage_key` → "FK to bronze.lineage_run.lineage_key" ✓

All FK references are specific to the table.column level, not just table-level.

### Section 5 — Glossary and Table Index

**Score: 5 / 5**

A Glossary section is present with definitions for: SCD-2, surrogate key, lineage_key, watermark, DQ rejection, CDF, IDENTITY, is_current_row. A Table Index lists all 7 covered tables with their schema and purpose. Both are well-formed.

---

## Cross-File Consistency Checks

**design.md DDL alignment:**
- `silver_fact.fact_purchase`: 11 columns in data-dictionary exactly match the DDL in design.md §7 ✓
- `bronze.purchase_staging`: 15 columns; data-dictionary includes `purchase_staging_key` (IDENTITY PK), matching design.md ✓
- `bronze.lineage_run`: 9 columns match design.md DDL ✓
- Column nullability for `received_outers` (NULL) and `is_order_finalized` (NOT NULL) match design.md ✓

**to-be.md ER diagram alignment:**
- `bronze.dq_rejections`: uses `rule_id` (not `dq_rule_id`) and `violation_column` (not `affected_column`). The to-be.md ER diagram confirms: `rule_id STRING` and `violation_column STRING` — trainee is consistent with their own to-be specification ✓
- `silver_dim.date` externally owned: confirmed by to-be.md §3.2 "silver_dim.date is a read-only reference calendar table not owned by the Purchase workflow" — partial justification for exclusion, but FK dependency still exists

**Runbook consistency:** The pipeline-runbook.md uses `assertion_name` in one DQ query (Section 2.5) but `rule_id` in another (Section 5). The data-dictionary defines the column as `rule_id` (not `assertion_name`). The data-dictionary is correct; the runbook has an internal inconsistency that contradicts the data-dictionary.

---

## Auto-Deducts

| Rule | Applied | Amount | Reason |
|---|---|---|---|
| Missing silver_dim.date section | Yes | -5 | Required table for FK documentation; reference documents it; purchase product has FK dependency |

---

## Summary

The data-dictionary.md is a high-quality deliverable that exceeds the reference's column format by adding Business Meaning and Derivation/Source columns to every entry. All 5 SCD-2 tracking columns are correctly documented with DATE types in both dimension tables. FK references are specific and accurate at the table.column level. Column names in dq_rejections (rule_id, violation_column) are consistent with the to-be.md ER diagram. The only gap is the absence of silver_dim.date documentation, which is partially justified by its external ownership but still required for FK completeness documentation.

---

## Priority Actions

1. Add a silver_dim.date section documenting its 11-14 columns (date PK, date_key, calendar_year, calendar_month_number, etc.) with a note that this table is externally owned and not loaded by the Purchase workflow.
2. Strengthen Derivation/Source entries for etl_cutoff and lineage_run columns: specify that `lineage_key` is generated by `open_lineage_record()` in `nb_extract_watermark`, and `cutoff_time` is updated by `set_etl_cutoff()` in `migrate_staged_purchase_data`.
