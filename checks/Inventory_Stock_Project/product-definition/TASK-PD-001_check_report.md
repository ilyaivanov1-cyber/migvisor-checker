---
task_id: TASK-PD-001
skill: migvisor-task-checker-product-definition
trainee_file: ./Inventory_Stock_Project/products/Purchase/current/specifications/development_plan/product-definition.yaml
reference_file: ./reference/answers/module_5/development_plan/product-definition.yaml
generated: 2026-09-18
total_score: 77/100
grade: Good
---

# TASK-PD-001 Check Report

**Product:** Purchase  
**Trainee file:** `./Inventory_Stock_Project/products/Purchase/current/specifications/development_plan/product-definition.yaml`  
**Reference file:** `./reference/answers/module_5/development_plan/product-definition.yaml`  
**Generated:** 2026-09-18

---

## Score Summary

**Product Definition Score: 77/100**

| Section | Weight | Raw Score | Weighted | Status |
|---|---|---|---|---|
| 1. Identity & Metadata Block | 25 | 90/100 | 22.5 | ✓ |
| 2. Schema Definitions (outputPorts) | 25 | 75/100 | 18.75 | ✓ |
| 3. Input Ports (inputPorts) | 25 | 72/100 | 18.0 | ✓ |
| 4. Owner / Roles / Lifecycle | 25 | 72/100 | 18.0 | ✓ |
| **Subtotal** | | | **77.25** | |
| Auto-deducts | | | **0** | |
| **Total** | | | **77/100** | |

**Grade: Good**

> **Weight calculation:** N = 4, base_weight = floor(100/4) = 25, remainder = 0 → all sections 25 pts.

---

## Section Matching Log

| Reference Section | Matched | Match Type |
|---|---|---|
| apiVersion / kind / metadata | apiVersion / kind / metadata | Direct |
| spec.catalog | spec.catalog (`inventory_stock`) | Direct |
| spec.schemas (stg/dim/fact/mart) | spec.schemas (bronze/silver_dim/silver_fact) | Partial |
| spec.outputPorts | spec.outputPorts | Direct |
| x-inputPorts (JDBC source) | x-inputPorts (dimension refs) | Partial |
| spec.owner / spec.roles | spec.owner / spec.roles | Direct |
| spec.lifecycle | spec.lifecycle | Direct |
| spec.sla | spec.sla | Direct |

---

## Auto-Deducts Applied

No systematic auto-deducts applied — gaps reflected in section scores.

**Total auto-deducts: 0 pts**

---

## Section Feedback

### 1. Identity & Metadata Block — 90/100 (weight 25 → 22.5 pts)

**Status:** ✓ Present

**Strengths:** ODPS 4.1 schema version correct. `kind: DataProduct` correct. Metadata block covers name (`purchase`), version, description, domain, product_type, source_system, and tags. Description accurately captures the medallion architecture, MERGE pattern, and lineage tracking approach. Tags include all relevant keywords (purchase, procurement, delta-lake, unity-catalog, scd2, watermark). Catalog `inventory_stock` is correct for this project.

**Gaps:** Reference metadata includes `created_at` and `updated_at` ISO-8601 timestamps — trainee omits these lifecycle timestamps. Minor difference in description verbosity (reference is more concise, trainee is richer — both acceptable).

---

### 2. Schema Definitions (outputPorts) — 75/100 (weight 25 → 18.75 pts)

**Status:** ✓ Present

**Strengths:** Three schemas defined: `bronze` (landing and control tables), `silver_dim` (conformed dimensions), `silver_fact` (analytical fact layer). Each schema has name, description, and layer fields. `silver_fact.fact_purchase` is correctly identified as the primary output port with grain documented (one row per purchase order line × supplier × stock item × order date). Port type `delta_table` correct.

**Gaps:** Reference defines 4 schemas: `stg`, `dim`, `fact`, `mart`. The `mart` schema (serving layer with materialized views for BI consumption) is entirely absent from trainee's product definition. This means the product definition does not declare the serving layer as an output port — BI consumers cannot discover the mart views through the data catalog. The naming difference (`bronze/silver_dim/silver_fact` vs `stg/dim/fact`) is project-specific and not penalized. Reference also includes `output_format: delta` and `access_method: unity_catalog` fields in each outputPort — trainee omits these.

---

### 3. Input Ports (inputPorts) — 72/100 (weight 25 → 18.0 pts)

**Status:** ✓ Present

**Strengths:** Trainee defines 3 x-inputPorts:
- `purchase_staging` — source system extract (SQL Server WideWorldImporters)
- `supplier_dimension` — SCD-2 dimension dependency with explicit `wwi_supplier_id` join key
- `stock_item_dimension` — SCD-2 dimension dependency with explicit `wwi_stock_item_id` join key

The dimension input ports with explicit join keys are a value-add over the reference — they document the interface contract between the Purchase product and the dimension teams.

**Gaps:** Reference's x-inputPorts section uses the JDBC connection pattern: source is described as a direct JDBC connection to the SQL Server `WideWorldImporters` database, specifying driver class, connection URL pattern, and credentials via Databricks Secrets scope. Trainee's `purchase_staging` inputPort describes the SQL Server source but does not specify JDBC driver, credentials path, or Secrets scope — these are critical for the extract notebooks to be correctly generated by `/13_migvisor_smartbuilder_generate-etl`. The `credentials_path` and `jdbc_driver` fields are absent from all inputPorts.

---

### 4. Owner / Roles / Lifecycle — 72/100 (weight 25 → 18.0 pts)

**Status:** ✓ Present

**Strengths:** Owner block specifies team, email, Slack channel, and on-call rotation. Roles block distinguishes ETL role (MODIFY on bronze/silver tables) from BI role (SELECT only on silver_fact, silver_dim). Lifecycle block specifies `status: active`, `start_date`, `review_date`. SLA block specifies `freshness_sla_hours: 6` and `availability_sla_percent: 99.5`. These are all present and correct.

**Gaps:** Reference roles section additionally specifies Unity Catalog GRANT syntax per role — trainee uses logical role names without UC GRANT pattern. Reference SLA includes an `rto_hours` (recovery time objective) and `rpo_hours` (recovery point objective) — trainee omits RTO/RPO. Reference lifecycle section includes `deprecation_date` (null by default) — trainee omits. These are minor omissions that reduce the YAML's completeness as a deployable artifact.

---

## Improvement Items

| # | Gap | Section | Est. Points |
|---|---|---|---|
| 1 | Add `mart` schema to outputPorts with materialized view descriptions for BI consumption | §Schemas | +6 pts |
| 2 | Add JDBC connection parameters to purchase_staging inputPort: jdbc_driver, jdbc_url_pattern, credentials_path (Secrets scope) | §inputPorts | +5 pts |
| 3 | Add access_method and output_format fields to each outputPort schema | §Schemas | +3 pts |
| 4 | Add RTO/RPO hours to SLA block | §SLA | +2 pts |
| 5 | Add created_at / updated_at lifecycle timestamps to metadata block | §Metadata | +1 pt |

---

## Priority Actions

1. **Add mart schema outputPort** — declare `silver_mart` (or `mart`) schema in outputPorts with at minimum the two serving views (`v_purchase_by_supplier` and `v_purchase_per_stock_item`) as discoverable data catalog entries. Without this, the serving layer is invisible to data catalog consumers. Worth up to **+6 pts**.
2. **Add JDBC connection details to purchase_staging inputPort** — specify `jdbc_driver: com.microsoft.sqlserver.jdbc.SQLServerDriver`, `credentials_path: {{secrets_scope}}/jdbc_url`, `credentials_scope: inventory-stock-dev` (dev) / `inventory-stock-prod` (prod). The generate-etl skill needs this to produce correct JDBC credential extraction code. Worth up to **+5 pts**.
3. **Add access_method and output_format to outputPorts** — standardize each port entry with `output_format: delta` and `access_method: unity_catalog`. Worth up to **+3 pts**.

---

*Report generated by migvisor-task-checker-product-definition on 2026-09-18*
