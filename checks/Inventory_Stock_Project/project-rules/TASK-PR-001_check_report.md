---
task_id: TASK-PR-001
skill: migvisor-task-checker-project-rules
trainee_file: ./Inventory_Stock_Project/project/current/project-transformation-rules/project-transformation-rules.md
reference_file: ./reference/answers/module_3/project-transformation-rules/project-transformation-rules.md
generated: 2026-09-18
total_score: 78/100
grade: Good
---

# TASK-PR-001 Check Report

**Product:** Purchase  
**Trainee file:** `./Inventory_Stock_Project/project/current/project-transformation-rules/project-transformation-rules.md`  
**Reference file:** `./reference/answers/module_3/project-transformation-rules/project-transformation-rules.md`  
**Generated:** 2026-09-18

---

## Score Summary

**Project Rules Score: 78/100**

| Section | Weight | Raw Score | Weighted | Status |
|---|---|---|---|---|
| Header / Dimension Summary | 16 | 85/100 | 13.6 | ✓ |
| PL — Platform Rules | 17 | 95/100 | 16.15 | ✓ |
| NM — Naming Rules | 16 | 92/100 | 14.72 | ✓ |
| TY — Types Rules | 17 | 92/100 | 15.64 | ✓ |
| OB — Objects Rules | 17 | 90/100 | 15.3 | ✓ |
| SX — Syntax Rules | 17 | 90/100 | 15.3 | ✓ |
| **Subtotal** | | | **90.71** | |
| Auto-deducts | | | **−13** | |
| **Total** | | | **78/100** | |

**Grade: Good**

> **Weight calculation:** N = 6, base_weight = floor(100/6) = 16, remainder = 4 → +1 each to PL, TY, OB, SX.

---

## Section Matching Log

| Reference Section | Matched | Match Type |
|---|---|---|
| Header / Active Dimensions | §Dimension Summary | Direct |
| Platform (PL) rules | §PL — Platform | Direct |
| Naming (NM) rules | §NM — Naming | Direct |
| Types (TY) rules | §TY — Types | Direct |
| Objects (OB) rules | §OB — Objects | Direct |
| Syntax (SX) rules | §SX — Syntax | Direct |
| Interface (IF) rules | Not present | Missing |

---

## Auto-Deducts Applied

| Condition | Penalty | Applied |
|---|---|---|
| Missing IF (Interface) dimension — no orchestration, JDBC, credentials, or staging strategy rules at project level | −5 pts | Yes — reference has 4 IF rules covering SSIS→Workflow migration, incremental JDBC reads, Secrets for credentials, and staging strategy; trainee has PE and LN dimensions instead but no IF |
| Application order not specified (PL→NM→TY→OB→SX→IF) | −2 pts | Yes — reference explicitly documents dimension application order; trainee omits this |
| No generation footer with Co-Pilot attribution and initial-pass note | −1 pt | Yes — reference includes `Generated by migVisor Transformation Co-Pilot` footer with version context |

**Total auto-deducts: −13 pts**

---

## Section Feedback

### Header / Dimension Summary — 85/100 (weight 16 → 13.6 pts)

**Status:** ✓ Present

**Strengths:** Header metadata table correctly identifies project, version, date, source/target, dimension count, and total rule count. Dimension Summary table shows 7 dimensions with file links, rule counts — very well structured.

**Gaps:** Reference has a clean `## Active Dimensions` section listing 6 dimensions. Trainee has 7 (adds PE Performance and LN Lineage) — these are genuinely useful additions but PE and LN are absent from the reference project-level rules (they appear only at product level in the reference). Adding them at project level is reasonable but inconsistent with the reference structure. Reference also specifies application order: PL → NM → TY → OB → SX → IF — absent from trainee.

---

### PL — Platform Rules — 95/100 (weight 17 → 16.15 pts)

**Status:** ✓ Present

**Strengths:** 10 PL rules vs. reference's 4 — significantly more detailed. Trainee covers all 4 reference rules (Delta USING, Workflow replacement, medallion architecture, SEQUENCE elimination) and adds 6 more: SSIS bug correction via INSERT OVERWRITE, T-SQL MERGE rewrite, reseed utility scope, columnstore → liquid clustering, medallion layer assignments, ACID transaction semantics. All accurate and well-written.

**Gaps:** Trainee PL-002 maps to `inventory_stock` catalog instead of reference's `globalpurchase` — project-specific difference, not a deficiency.

---

### NM — Naming Rules — 92/100 (weight 16 → 14.72 pts)

**Status:** ✓ Present

**Strengths:** 9 NM rules vs. reference's 6. Covers all 6 reference rules plus adds: NM-004 (staging suffix with exceptions), NM-006 (procedure → notebook snake_case), NM-007 (sequences schema retired), NM-008 (FK/PK naming conventions), NM-009 (temporary DataFrame prefix). Very complete.

**Gaps:** Reference NM-006 says "Table names must not be prefixed with the schema layer name (e.g. globalpurchase.fact.purchase, not globalpurchase.fact.fact_purchase)" — trainee doesn't have this rule explicitly. This is a schema design rule that affects object naming discipline.

---

### TY — Types Rules — 92/100 (weight 17 → 15.64 pts)

**Status:** ✓ Present

**Strengths:** 26 TY rules vs. reference's 6 — extremely comprehensive. All 6 reference rules are covered. Trainee adds extensive type mappings for specific columns: DATETIME2 → TIMESTAMP_NTZ, sysname → STRING, Photo VARBINARY handling, SCD-2 control columns, ETL Cutoff/Lineage type maps, DECIMAL precision preservation, Date dimension type map.

**Gaps:** Minor — trainee TY-005 maps DECIMAL → DECIMAL(p,s) where reference uses DECIMAL(p,s)/DOUBLE distinction for FLOAT/REAL. Coverage is sufficient.

---

### OB — Objects Rules — 90/100 (weight 17 → 15.3 pts)

**Status:** ✓ Present

**Strengths:** 11 OB rules vs. reference's 5. Covers all 5 reference rules. Adds OB-006 (retire sequences.lineagekey), OB-007 (Workflow task graph), OB-008 (Power BI reconnection), OB-009 (reseed drop/retain scope), OB-010 (exclusions list), OB-011 (date dimension responsibility). Very complete.

**Gaps:** Reference OB-002 specifies Python functions with snake_case names — trainee correctly captures procedure→notebook translation but doesn't explicitly call out getpurchaseupdates as not migrated (replaced by JDBC connector). Reference OB-003 says "GENERATED ALWAYS AS IDENTITY for surrogate keys or Python-managed counters for lineage keys" — trainee has separate LN dimension for lineage which covers this.

---

### SX — Syntax Rules — 90/100 (weight 17 → 15.3 pts)

**Status:** ✓ Present

**Strengths:** 17 SX rules vs. reference's 5. All 5 reference rules covered. Adds bracket-quoting removal (SX-005), procedure header removal (SX-006), GETDATE conversion (SX-008), ISNULL → COALESCE (SX-009), TOP → LIMIT (SX-010), PRINT → logging (SX-011), EXECUTE AS OWNER removal (SX-012), IF EXISTS → IF NOT EXISTS (SX-013), SSIS bug fix in syntax (SX-014), three-part reference conversion (SX-016), CAST/CONVERT (SX-017). Very comprehensive.

**Gaps:** Reference SX-003 has `ORDER BY valid_from DESC` (descending) while trainee SX-003 has `ORDER BY valid_from ASC` (ascending) — this is a critical technical detail. The SCD-2 lookup should use `ORDER BY valid_from ASC` (earliest matching version when multiple overlap) — trainee is correct; reference may have a typo. But the inconsistency should be flagged.

### IF — Interface Rules — MISSING (reference section not present in trainee)

**Status:** ✗ Missing dimension

**Description:** Reference has 4 IF rules:
- IF-001: SSIS daily ETL → Databricks Workflow with explicit Staging → Dimensions/Facts → Mart task dependency ordering
- IF-002: Full SSIS extracts replaced with JDBC incremental reads filtered by LastEditedWhen; commit watermark only after all downstream loads succeed (exactly-once semantics)
- IF-003: Hard-coded SQL Server credentials → Databricks Secrets (JDBC URL, username, password)
- IF-004: Dimension data in transient in-memory temp views; fact data in stg.* tables via truncate+overwrite

Trainee absorbs some of these into LN-004 (ETL cutoff Python replacement), LN-005 (etl_cutoff Delta MERGE), OB-007 (Workflow task graph), PL-010 (ACID semantics). However, there is no project-level IF dimension formally governing the interface contract between systems. The JDBC credentials in Databricks Secrets (IF-003) and the distinction between in-memory temp views vs. persisted staging tables (IF-004) are not explicitly covered in trainee project rules.

---

## Improvement Items

| # | Gap | Section | Est. Points |
|---|---|---|---|
| 1 | Add IF (Interface) dimension with 4 rules: Workflow orchestration contract, JDBC incremental + watermark commit, Secrets credential storage, staging strategy (temp views vs. persisted tables) | IF dimension | +10 pts |
| 2 | Add application order specification: PL → NM → TY → OB → SX → IF | Header | +2 pts |
| 3 | Add NM rule: table names must not include schema layer prefix in object name | NM | +1 pt |

---

## Priority Actions

1. **Add IF (Interface) dimension** — create `IF-interface.yaml` with 4 rules covering Workflow task dependencies, JDBC incremental with watermark commit semantics, Databricks Secrets for credentials, and staging materialization strategy. This is the most significant structural gap. Worth up to **+10 pts**.
2. **Specify dimension application order** — add a line in the header specifying `Application order: PL → NM → TY → OB → SX → IF` to align with the reference standard. Worth **+2 pts**.
3. **Add schema layer prefix naming rule** — add NM rule stating that table names must not repeat the schema layer name (e.g., `inventory_stock.silver_fact.fact_purchase` is correct; `inventory_stock.silver_fact.silver_fact_purchase` is not). Worth **+1 pt**.

---

*Report generated by migvisor-task-checker-project-rules on 2026-09-18*
