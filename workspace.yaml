# migvisor-as-is-checker workspace configuration
trainee_workspace: auto        # auto = scan root for any folder with products/ + project/ structure
reference_workspace: ./reference/answers
product: Purchase
checks_dir: ./checks
